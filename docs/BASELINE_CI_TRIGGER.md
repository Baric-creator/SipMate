# Baseline validation trigger

This marker exists only to produce a fresh pull-request CI run after the sanitized historical schema baseline was added. Remove or squash it when the baseline validation work is consolidated.